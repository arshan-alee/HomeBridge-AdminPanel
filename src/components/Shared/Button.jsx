import React from "react";

const Button = ({ text }) => {
  return (
    <button className="bg-[#111C44] text-white px-10 py-4 font-bold rounded-3xl">
      회원생성
    </button>
  );
};

export default Button;
